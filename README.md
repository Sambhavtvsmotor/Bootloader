# BootLoader

BootLoader : Bootloader is nothing but a small piece of code stored in MCU flash or ROM to act as an application loader as well as a mechanism to update the applications whenever required.
